# =============================================================================
# Environment Variables for Discord OAuth + Supabase
# =============================================================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration (Frontend)
# -----------------------------------------------------------------------------
# Get these from your Supabase project settings > API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# -----------------------------------------------------------------------------
# Discord OAuth Configuration (Frontend)
# -----------------------------------------------------------------------------
# Get this from Discord Developer Portal > Your App > OAuth2
# SECURITY: Only CLIENT_ID goes here - CLIENT_SECRET is in Edge Function only!
VITE_DISCORD_CLIENT_ID=your_discord_client_id

# -----------------------------------------------------------------------------
# Optional: Role-based Access Configuration
# -----------------------------------------------------------------------------
# Discord server (guild) ID for role verification
VITE_EGGINC_GUILD=your_guild_id

# Discord role IDs for admin access (optional)
VITE_EGGINC_WONKY_LEADER_ROLE=your_wonky_leader_role_id
VITE_EGGINC_MAJ_ROLE=your_maj_role_id

# =============================================================================
# Edge Function Secrets (Set in Supabase Dashboard, NOT here!)
# =============================================================================
# These environment variables must be set in the Supabase Dashboard:
# Dashboard > Edge Functions > discord-auth > Secrets
#
# Required secrets:
#   DISCORD_CLIENT_ID     - Same as VITE_DISCORD_CLIENT_ID
#   DISCORD_CLIENT_SECRET - From Discord Developer Portal (NEVER expose!)
#   JWT_SECRET            - From Supabase > Settings > API > JWT Secret
#   SUPABASE_URL          - Your Supabase project URL (same as VITE_SUPABASE_URL but without https://)
#
# How to set these:
#   supabase secrets set DISCORD_CLIENT_ID="your_client_id"
#   supabase secrets set DISCORD_CLIENT_SECRET="your_client_secret"
#   supabase secrets set JWT_SECRET="your_jwt_secret_from_dashboard"
#   supabase secrets set SUPABASE_URL="https://your-project.supabase.co"
#
# After setting secrets, redeploy the Edge Function:
#   supabase functions deploy discord-auth
#
# These secrets are used by the Edge Function to:
# 1. Exchange OAuth codes for Discord access tokens (CLIENT_SECRET)
# 2. Sign JWTs with proper issuer claim (SUPABASE_URL)
# 3. Sign JWTs that authenticate users to Supabase (JWT_SECRET)
# =============================================================================
